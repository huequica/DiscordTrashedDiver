import { TWITTER_TOKENS } from '@/config/env';

export const mockTwitterTokens: ReturnType<typeof TWITTER_TOKENS> = {
  consumer: {
    key: 'ssss',
    secret: 'ssss',
  },
  account: {
    key: 'ssss',
    secret: 'ssss',
  },
};
